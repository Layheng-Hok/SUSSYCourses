server:
  port: 8081
  error:
    include-message: always
    include-binding-errors: always

spring:
  datasource:
    username: layhenghok
    password:
    url: jdbc:postgresql://localhost:5432/cs309_project
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: henglayhok@gmail.com
    password: esdd zmwj lxji hsrl
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

logging:
  level:
    org:
      springframework:
        security: DEBUG
